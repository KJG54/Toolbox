# Sprite animation planning

Group a local Toolbox atlas layout into animation frame lists based on the filename prefix:

```powershell
toolbox game sprite-animation-plan outputs/sprites.atlas.json --output outputs/animations.json
```

Review the grouping before creating engine-specific animation resources.
