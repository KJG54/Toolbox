# Procedural game audio

Generate original deterministic local WAV prototypes for a music loop or common game SFX. The results are procedural, not AI-generated.

```powershell
toolbox audio procedural --kind loop --duration 12 --output outputs/loop.wav
toolbox audio procedural --kind coin --output outputs/coin.wav
```
