# Duplicate finder

Report exact duplicates and visually similar local images without deleting anything:

```powershell
toolbox catalog find-duplicates assets --threshold 4
```

Treat near matches as review candidates, not automatic deletion decisions.
