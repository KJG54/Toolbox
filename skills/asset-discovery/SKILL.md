# Asset discovery

Search only the locally reviewed source catalog:

```powershell
toolbox research assets "stone wall texture" --kind texture --commercial
```

The result is a source recommendation, not a download. Review the selected asset's license before
acquisition, especially when a source supports multiple contributor-selected licenses.
