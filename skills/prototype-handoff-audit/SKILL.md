# Prototype handoff audit

Check a local Godot project for asset provenance and optionally require a local build artifact:

```powershell
toolbox game prototype-handoff my-godot-game --require-build
```

This is a structural handoff check, not a substitute for running the game or engine export validation.
