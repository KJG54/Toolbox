# Procedural Blender props

Create a simple original prototype prop in a new Blender file:

```powershell
toolbox blender procedural-prop --kind crate --output outputs/crate.blend
```

Inspect and refine the result before game import.
