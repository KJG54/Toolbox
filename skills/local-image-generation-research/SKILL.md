# Local image-generation research

Review the current local candidate without downloading code or weights:

```powershell
toolbox research generation image
toolbox research evaluate-generation image
```

The assessment reports access, license, and current hardware fit. It never installs a runtime,
downloads model files, or substitutes a cloud service.
