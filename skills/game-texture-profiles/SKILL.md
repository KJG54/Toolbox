# Game texture profiles

Export a source-preserving texture derivative for a local target profile:

```powershell
toolbox texture export-profile texture.png --profile mobile --output outputs/texture.webp
```

Profiles change only size and output encoding; they do not generate detail or alter the source.
