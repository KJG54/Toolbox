# Audio mixing

Mix local narration over a music bed with gain, fade-in, and ducking:

```powershell
toolbox audio mix --narration narration.wav --bed music.wav --output outputs/mix.wav
```

This creates a derivative only; it does not generate or upload audio.
