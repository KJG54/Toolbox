# Texture and material validation

Inspect texture dimensions, alpha, and conventional material-map filenames locally:

```powershell
toolbox texture validate textures
```

The result is readiness evidence, not a guarantee that a material renders correctly. It does not
rename, resample, or generate texture maps.
