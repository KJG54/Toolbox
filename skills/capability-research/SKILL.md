# Capability research

Start with a deterministic local route:

```powershell
toolbox research gap CAPABILITY --commercial --free
```

If it reports `CAPABILITY_GAP`, return the generated review brief. Research only official
project and license sources, then ask for approval before adding a registry record, installing
software, downloading a model, or invoking an external service.
