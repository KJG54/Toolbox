# Generation candidate evaluation

Assess currently reviewed image, music, or SFX candidates against this machine:

```powershell
toolbox research evaluate-generation music
toolbox research evaluate-generation sfx
```

Treat the output as an installation gate. It records no benchmark claim, does not download models,
and reports a hardware or runtime upgrade requirement instead of suggesting remote execution.
