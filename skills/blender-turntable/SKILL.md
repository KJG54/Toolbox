# Blender turntable

Render a local `.blend` scene from multiple orbiting angles:

```powershell
toolbox blender turntable asset.blend --output-dir outputs/turntable --frames 12
```

The source file is never saved; inspect the frame sequence before publishing it.
