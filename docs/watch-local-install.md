# Watch Skill — canonical local install

The original local installation note is preserved here after the Toolbox migration. The
source now lives at `components/watch-skill`; its working tree was imported unchanged and
the original repository history is preserved in `archives/watch-skill-pre-toolbox.bundle`.
