# Adding a Registry Record

Use a capability-first record. Add the capability, license source, tool metadata, adapter
path when needed, and routing requirements. Run `python -m toolbox.registry` validation
through the test suite before review. Mark unverified licensing as `requires-review`; it
must not become a commercially safe recommendation.
